/**
 * Inicialización de Sentry para monitoreo de errores en producción.
 * Stub por defecto para que el build funcione sin instalar @sentry/react.
 *
 * Para activar Sentry:
 * 1) npm install @sentry/react
 * 2) Sustituir el contenido de este archivo por el de docs/SENTRY_SETUP.md (sección lib/sentry.ts)
 */
export function initSentry(): void {
  // Stub: no-op si @sentry/react no está instalado. Ver docs/SENTRY_SETUP.md para activar.
}
